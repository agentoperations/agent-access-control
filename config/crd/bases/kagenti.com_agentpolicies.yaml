---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: agentpolicies.kagenti.com
spec:
  group: kagenti.com
  names:
    kind: AgentPolicy
    listKind: AgentPolicyList
    plural: agentpolicies
    shortNames:
    - ap
    singular: agentpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.matchedAgentCards
      name: Matched
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentPolicy is the Schema for the agentpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentPolicySpec defines the desired state of AgentPolicy.
            properties:
              agentSelector:
                description: AgentSelector selects the AgentCards this policy applies
                  to.
                properties:
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: MatchLabels is a map of key-value pairs used to match
                      AgentCards.
                    type: object
                required:
                - matchLabels
                type: object
              agents:
                description: Agents is a list of agent names that this policy applies
                  to.
                items:
                  type: string
                type: array
              external:
                description: External defines the policy for external service access
                  and credential management.
                properties:
                  defaultMode:
                    default: deny
                    description: DefaultMode is the default credential handling mode
                      applied when no rule matches.
                    enum:
                    - vault
                    - exchange
                    - passthrough
                    - deny
                    type: string
                  rules:
                    description: Rules defines per-host external access rules.
                    items:
                      description: ExternalRule defines the credential handling policy
                        for a specific external host.
                      properties:
                        audience:
                          description: Audience is the intended audience for token
                            exchange.
                          type: string
                        header:
                          default: Authorization
                          description: Header is the HTTP header name used to inject
                            the credential.
                          type: string
                        headerPrefix:
                          default: 'Bearer '
                          description: HeaderPrefix is the prefix prepended to the
                            credential value in the header.
                          type: string
                        host:
                          description: Host is the external hostname this rule applies
                            to.
                          type: string
                        mode:
                          description: Mode is the credential handling mode for this
                            host.
                          enum:
                          - vault
                          - exchange
                          - passthrough
                          - deny
                          type: string
                        scopes:
                          description: Scopes is the list of OAuth scopes to request
                            during token exchange.
                          items:
                            type: string
                          type: array
                        vaultPath:
                          description: VaultPath is the path in a vault where credentials
                            for this host are stored.
                          type: string
                      required:
                      - host
                      - mode
                      type: object
                    type: array
                required:
                - defaultMode
                - rules
                type: object
              ingress:
                description: Ingress defines the ingress access control policy.
                properties:
                  allowedAgents:
                    description: AllowedAgents is a list of agent names permitted
                      to communicate with the selected agents.
                    items:
                      type: string
                    type: array
                  allowedUsers:
                    description: AllowedUsers is a list of user identifiers permitted
                      to communicate with the selected agents.
                    items:
                      type: string
                    type: array
                type: object
              mcpTools:
                description: MCPTools references the MCP tools virtual server for
                  this policy.
                properties:
                  virtualServerRef:
                    description: VirtualServerRef is the name of the VirtualServer
                      resource for MCP tools.
                    type: string
                required:
                - virtualServerRef
                type: object
              rateLimit:
                description: RateLimit defines rate limiting parameters for this policy.
                properties:
                  requestsPerMinute:
                    description: RequestsPerMinute is the maximum number of requests
                      allowed per minute.
                    minimum: 1
                    type: integer
                required:
                - requestsPerMinute
                type: object
            required:
            - agentSelector
            type: object
          status:
            description: AgentPolicyStatus defines the observed state of AgentPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the AgentPolicy's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              generatedResources:
                description: GeneratedResources lists the Kubernetes resources generated
                  by this policy.
                items:
                  description: GeneratedResourceRef is a reference to a Kubernetes
                    resource generated by the controller.
                  properties:
                    kind:
                      description: Kind is the Kubernetes resource kind.
                      type: string
                    name:
                      description: Name is the name of the generated resource.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              matchedAgentCards:
                description: MatchedAgentCards is the number of AgentCards matched
                  by the selector.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
